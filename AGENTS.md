# codex-learning-os

这是只供本人使用的 Markdown-first 学习与科研辅助系统。目标是提高未来的独立
学习能力，不是让当前的 AI 辅助答案看起来完整。

## 入口与规则分工

1. 先读 `SPEC.md`；每个新对话首次任务按其中的同步规则检查工作区、当前分支及
   upstream，执行 `git pull --ff-only`，更新后重读有变化的相关规则。同步受阻时
   保留本地修改，说明阻塞并继续不依赖同步的工作，不宣称已同步。
2. 学习与科研任务遵循 `.agents/skills/ai-tutor/SKILL.md`，按需读取相关
   `Concepts/`、`Papers/`、`Mistakes/` 和同 `thread` 的最近 `Sessions/`。
   项目维护只读取相关规则与文件，无需套用教学测评流程或创建学习记录。
3. 写学习记录时遵循 `Templates/` 的必需字段与固定栏目；未用可选栏目可省略，
   保留学习者自己的原始思考。

本文件是简短操作入口；状态、证据、路径和 Git 的长期规则以 `SPEC.md` 为准，
Tutor 技能负责教学操作。不要复制 SPEC 或重新设计系统。用户当次明确要求优先于
技能的默认建议；仍须遵守系统和工具权限。技能若导致暂停、额外确认或偏离任务，
指出实际读取的文件路径与具体条款，并区分明确要求和自己的推断。

## 执行与协作

- 对明确的维护、整理、修复请求直接完成相关修改与检查；依据上下文处理常规、
  可逆的细节，不为已有授权重复确认。会实质改变目标或覆盖原始内容的歧义才简短
  询问，同时继续不依赖答案的工作。尚未授权的破坏性操作须先确认。
- 需要学习者尝试或正式测评时，按 Tutor 规则等待真实作答；浏览、直接讲解及
  已授权实验按对应规则推进，用户暂停时尊重其意图。不代写 learner attempt 或
  提前泄露 assess 答案；AI 分析明确标为 Tutor 建议，不作为学习者证据。
- 中途补充要求时保留已完成工作并调整后续步骤；回答插入问题后继续原任务，除非
  用户明确停止或改变目标。阻塞时说明已完成部分、实际原因及所需下一步。
- 默认单 Agent；仅在用户明确要求时使用子 Agent，限定独立子任务和写入范围，
  由主 Agent 整合检查，避免并行修改同一记录。

## 学习与记录边界

- Markdown + YAML frontmatter 是状态来源；不主动加入复杂基础设施。
- assistance 不等于 mastery；核心学习在 A5 后邀请 learner reconstruction，浏览、明确暂停或只想听解释时不强制。
- 从自然语言推断 browse、learn、assess；正式 assess 先出题再收 confidence，缺少输出不推定不会。
- 重要 Mistake 先复用已有模式；不为小计算错误建档。
- `Sessions/` 用 `thread` 区分并行任务；resume 有歧义时只简短询问，不猜。
- 暂停可先保存真实检查点，不要求先 reflection；跨文件更新先保存 Session，再更新引用方。
- 新学习记录的 slug 使用 lowercase-kebab-case，文件路径遵循 SPEC；目录、规则
  文件和模板名称保持既有约定。
- 私人学习与科研内容默认按 private repository 处理，具体规则见 `SPEC.md`。

## 沟通与完成检查

- 默认用清晰、简洁的中文，先说结论或当前关键问题；解释深度按学习目标调整，
  保留必要条件、公式与例子。列表、表格仅用于有助理解的步骤或比较，避免套话和
  重复总结；保存的 Markdown 记录仍遵循模板。
- 检查与改动规模匹配：文档检查差异、规则一致性及涉及的路径或链接；学习记录
  加查 frontmatter、原始输出和证据引用；代码运行相关必要检查。通过后仅在出现
  新改动、失败或未解决疑点时扩大检查，不为小改动新增测试体系。
- 用户已于 2026-09-06 授权：完成修改并通过相应检查后，按 SPEC 自动 commit、
  push 本次相关改动到本仓库 GitHub remote，保留其他工作，不强推。最终简述
  改了什么、实际检查结果及提交/推送状态；未运行或未验证的内容如实说明。

维护参考：2026-09-07 核对 [OpenAI GPT-6 Astra 官方指南的 Prompting best practices](https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices)。
自主执行、指令边界、表达、委派和检查尺度已按本项目学习目标调整。
