# codex-learning-os 修改执行方案

日期：2026-09-05  
状态：待执行；本文件不代表修改已经完成。  
范围：个人 Markdown 学习与科研系统的规则、模板和使用说明。

## 1. 任务与完成定义

在保留现有 Markdown-first 架构的前提下，使系统可靠完成：

> 明确本次目标 → 给予合适帮助 → 保留关键原始输出 → 根据证据更新判断 → 留下可直接恢复的下一步。

执行完成必须同时满足：

1. 本方案规定的文件变更全部完成，规范、技能、模板和说明之间没有冲突。
2. 未评估不再默认等于不会；即时重建不再被标记为正式 A0 成功。
3. 重要能力判断可以追溯到学习者实际输出；没有输出时明确待验证。
4. 暂停可以保存检查点，不要求先完成重建或反思。
5. 浏览、学习、测评的行为可区分，但用户不必记住模式名称。
6. 完成第 10 节的验收，并提交含真实结果的验收报告。

文档一致性通过和模拟验收通过，只证明设计已落实；不得据此声称学习效果已经验证。

## 2. 基线、权限与保护范围

本方案依据当前工作区文件，而不是 Git HEAD。编写时工作区存在大量未提交修改、删除及 PDF 新文件。

执行前必须重新检查实际工作区，记录本次开始前的变更清单。已有修改属于用户当前基线，不能视为需要清理的问题。

必须遵守：

- 不执行全局 reset、checkout、clean，不恢复已删除的旧代码、测试或架构文档。
- 不覆盖用户原始回答，不从历史摘要推造原始回答，不把模拟回答写成用户表现。
- 不删除、重命名或搬移现有论文 PDF；本轮 lowercase-kebab-case 约束适用于 Markdown 记录。
- 不自动 commit 或 push，不修改远程仓库、依赖、插件或全局配置。
- `.agents` 等目录如受环境权限限制，使用正规授权机制；不得绕过限制。真实受阻时报告路径、原因和剩余工作。
- 若执行时发现新学习记录，按第 9 节兼容处理，不假定文件夹仍为空。

本方案授权范围是下述文档改造与验收，不包含建设新应用。执行者不应再次询问是否采用已明确规定的字段、模式或文件名。

## 3. 文件范围与职责

| 文件 | 必须修改的内容 | 职责边界 |
|---|---|---|
| `SPEC.md` | 模式、证据、掌握、暂停、复习、来源和兼容规则 | 长期规则的权威来源 |
| `.agents/skills/ai-tutor/SKILL.md` | 意图判断、读写顺序、提示行为、记录及恢复操作 | 执行流程，引用 SPEC，不重复整段定义 |
| `Templates/concept.md` | 未评估默认值、证据引用、下一步练习 | 当前学习状态及其依据 |
| `Templates/session.md` | 模式、检查点、可选关键证据 | 学习原始输出与恢复线索 |
| `Templates/paper.md` | 来源、blocker 验证、主张与验证路径 | 原文定位及阅读、研究状态 |
| `Templates/mistake.md` | 证据来源、最近复测、下一次测试 | 稳定错误模式及修复情况 |
| `README.md` | 使用例子、模式、暂停、证据边界 | 用户快速使用说明 |
| `AGENTS.md` | 仅在需要时修正入口指引 | 保持简短，不复制 SPEC |
| `acceptance-report.md` | 新增本轮实际验收报告 | 执行者填写，不作为学习记录 |

保留本方案用于交接，不把它加入 Tutor 每次启动必读项。没有必要的文件不要改。

本轮不新增数据库、CLI、调度器、Web UI、检索系统、插件、复杂知识图谱、自动评分器或独立证据文件夹。证据存放在现有 Session 中。

## 4. 必须落地的行为规则

### 4.1 根据目标选择行为

模式只描述本次活动，不是学习者的永久状态，也不是复杂状态机。

| 模式 | 意图示例 | 默认行为 | 证据边界 |
|---|---|---|---|
| `browse` | 查符号、了解背景、快速浏览、只想听解释 | 直接给必要说明，不强制测试 | 不据此提高能力分数或写 mastered |
| `learn` | 学会概念、修复推导、解决论文核心 blocker | 最低有效帮助，关键教学后邀请主动输出 | 即时输出只支持相应的当下理解判断 |
| `assess` | 测试是否会、闭卷测评、延迟复习测试 | 先出题、收置信度，再等待独立回答 | 满足正式 A0 条件才可作为独立证据 |

执行细则：

- 从自然语言和当前活动推断模式；意图明确时不先让用户选模式。
- “直接讲吧”允许立即 A5，不默认用户拒绝后续练习；“只想听解释，不要测试”则尊重其要求，不强制闭环。
- 用户随时可以暂停或切换；暂停不降低分数，拒绝回答不等于不会。
- 普通学习不要求每题报置信度。正式测评先收 0–100；用户不愿提供时记 `unknown`，不得补猜，也不因缺置信度阻止作答。
- 测评题先展示，再问作答前置信度，避免对尚未看到的问题估计置信度。
- 模式切换若对应新阶段，Session 保存必要活动顺序。frontmatter 的 mode 表示检查点时的模式；历史片段的帮助等级以片段本身为准。

### 4.2 A0 与证据类型

保留 A0–A5 的帮助等级定义；不把等级变成必须逐级经过的状态机。

关键证据使用以下四种类型，作为正文标签，不新增复杂对象系统：

| 类型 | 判定 | 能支持的结论 |
|---|---|---|
| `assisted` | 在提示或示例帮助下完成 | 当前所需帮助及局部表现 |
| `reconstruction` | 针对刚讲解过的内容，不看答案重新输出 | 当下理解、阅读 blocker 是否基本解除 |
| `independent` | 新测试任务，未获得针对该任务的提示或答案 | 本次独立表现 |
| `delayed-independent` | 不同日期对相关能力再次独立测试，未先复习答案或获得任务提示 | 跨日保留表现 |

陌生迁移另外记 `novel_transfer: yes / no / unknown`，不另造第五类，以便同一次输出同时体现延迟和迁移。

正式 A0 的必要条件：

1. 不查笔记、来源或 AI 答案；可记录为学习者自述，不能宣称系统监控过。
2. 当前题目及关键解法没有被本轮提前揭示；机械替换数字的同题不得充当陌生迁移。
3. 作答期间没有收到提示、下一步、例题或答案。
4. 原始输出和判断依据可追溯。

“曾学过这个知识点”不使未来所有测评失去资格；需要排除的是当前任务的直接答案泄露和作答帮助。

即时重建即使没有再次提示，也标 `reconstruction`；帮助字段写清“先 A5，重建时无新增提示”，不得只写 A0 掩盖教学上下文。

正式 A0 中途求助时，保留帮助前的原始输出，将后续片段标为实际 A1–A5；本次完整答对不计作独立成功。可另择新题重新测评，但不当场机械变式制造成功。

判分不确定时记录 `uncertain`，查对定义、条件或原文后再更新。不得为了更新分数把 AI 的不确定判断当作事实。

### 4.3 掌握状态与能力评分

- 四个能力维度仍为 recall、explanation、derivation、transfer。
- 新模板默认 `null`：尚未评估；`0` 只用于已有证据支持“当前不能完成该维度基本任务”。
- 暂不适用的维度也留 `null`，在正文解释原因；不为填满模板制造测试。
- `ai_level: null` 表示尚未评估；有实际观察后才写 A0–A5。
- 评分是粗略判断，不计算平均分，不用“总分达标”自动触发 mastered。
- 每次实质提高分数或改变状态，必须在 Concept 的 Evidence 中链接相应 Session，并解释依据。
- browse、AI 解释、用户说“懂了”、待回答、待重建不能单独提高分数。
- `learning`：尚在建立理解，或证据不足以判断可独立练习；不等于完全不会。
- `practicing`：已有实质输出支持基本理解，正在练习独立完成；必须有证据引用。

重要 Concept 标 `mastered` 的最低门槛：

1. 至少两次合格独立输出，发生在不同日期，其中至少一次为 delayed-independent。
2. 覆盖该概念当前目标的关键维度，并包含至少一个陌生迁移成功。
3. 没有未解释的关键反证或仍影响目标能力的概念错误。
4. 在 Concept 正文说明掌握的范围及证据日期，避免把局部任务成功扩大到整个领域。

跨日和次数是本项目的保守操作门槛，不是经验证的心理测量阈值。满足门槛仍不自动判 mastered；证据太窄或评分存疑时保持 practicing。

已 mastered 后出现有效反证，应保留历史证据并写明新的弱点；必要时退回 practicing。不要因为单次笔误或到期未复习自动降级。

### 4.4 暂停、保存与恢复

Session 使用状态 `active`、`paused`、`completed`，只表示活动状态，不表示概念掌握。

保存触发点：

- 完成一个有价值的小阶段；
- 切换主要主题；
- 用户明确暂停、结束或要求保存；
- 已给出关键教学并请求重建，适合留下等待状态时。

同一活动的阶段检查点更新当前 Session；已经结束或暂停的活动后来重新开始，则按原命名规则建立新 Session，并链接上一份。不得每轮问答新建文件。

保存不依赖用户完成 reflection。没有新理解就写“尚未输出”，没有测试就写“未测试”，不得代写学习者思想。

检查点至少回答：当前做到哪里、已经提供什么帮助、最后一次真实输出在哪里、正在等什么、恢复后的第一步是什么。

“下一步”要可直接执行，例如“请不看刚才的推导，解释式 (7) 中消去 nuisance parameter 的条件”；不要只写“继续学习 EFIM”。需要来源时附论文页码或公式位置。

恢复顺序：确定 thread → 读最近 Session → 按链接读相关 Concept/Paper/Mistake → 执行下次第一件事。不重复整段教学，不重新要求已提供的信息。

两个合理线程同时存在时，只简短消歧；不以文件修改时间猜用户意图。paper:cross-band-fm 对应文件名 thread-slug 为 paper-cross-band-fm，frontmatter 保留原 thread。

不能承诺捕捉没有工具调用机会的突然关机或应用退出；检查点仅覆盖实际成功写入的内容。

### 4.5 论文 blocker、来源与研究判断

- 每篇有实际笔记的 Paper 记录已知标题、来源链接或本地 PDF、版本信息和当前位置；不知道的信息写 UNKNOWN，不根据文件名编造作者、出版状态或 DOI。
- 不为 14 份 PDF 批量创建空笔记，只在真实阅读时创建。
- 保留 P0–P3、blocker 类型和 L1–L5。
- 补课后回原文做 reconstruction；正确输出可以标记“当前阅读 blocker 基本解除”，并链接证据。
- blocker 解除不代表 prerequisite 已 mastered，也不要求先等待跨日测评才继续论文。
- 解释完但未作答时保持 open。用户选择暂时跳过时标明“暂缓、未验证”，不能勾选为已解决。
- 来源位置、作者主张、学习者推断、AI 建议分别标清；评审和实验结果不能相互冒充。
- 对重要研究疑问写一个可执行验证动作。该动作只是计划，不自动授权下载、跑实验或宣称结论已验证。

### 4.6 最小复习规则

不新增 scheduler。继续利用 `next_review` 和自然语言询问。

- 只给重要概念或正在反复出错的能力安排复习，不要求每个术语都有日期。
- 当即时重建成功、值得检查保留时，可默认把首次延迟检查建议设在 2–3 天后；用户时间和当前任务优先。这是可调整的起点，不是最佳间隔的科学结论。
- 失败时安排修复与新测试，成功时酌情延后。不要建立固定复杂算法。
- “今天最值得学什么”先尊重用户当前目标；缺少目标时优先当前论文 P0，再考虑到期的关键复习或重复错误，每次只推荐一个具体动作。
- 读取 frontmatter 及必要的 Next Practice、复测和下一步段落即可，不加载全部正文。
- 到期但未完成只代表待复习，不自动推断遗忘。

### 4.7 写入和错误容错

- 新记录使用模板，但保留既有记录正文及用户措辞。Tutor 总结、正确模型和用户原话分开。
- 确定性的小格式错误可以最小修复并说明，例如新可选字段缺失。不能因为未填可选字段停止教学。
- 无法可靠解析的 frontmatter 或 thread 冲突：不猜状态、不覆盖相关记录；可继续不依赖该状态的解释，简短指出需澄清的信息。
- 名称明确且没有同义候选时直接创建记录并告知，无须只为确认 slug 中断。确有重复候选时简短消歧。
- Session 保存原始证据，Concept 保存当前总结与证据链接。跨文件更新先保存 Session，再更新引用方；失败时报告部分完成，不能宣称已全部保存。
- 不追求多文件事务或自动恢复基础设施；通过顺序写入和清楚的检查点控制风险。

## 5. 模板修改契约

以下字段与标题为本轮约定；执行者按此落地，不自行增加必填字段。现有有用正文栏目保留，明确要求合并的重复栏目除外。可选栏目无内容时可以留空，不诱导 AI 填造事实。

### 5.1 Templates/concept.md

frontmatter：

```yaml
---
status: learning
recall: null
explanation: null
derivation: null
transfer: null
ai_level: null
weakness: []
next_review: null
prerequisites: []
---
```

保留当前五个正文栏目，并增加：

- `## Learning Target`：当前希望独立完成什么，哪些维度暂不适用。
- `## Evidence`：Session 相对链接、日期、相关维度、证据类型、支持或反驳的判断。不要复制完整回答。
- `## Next Practice`：下一次实际要练什么；正式测评不得在这里保存答案或解题提示。

Evidence 必须容纳反证，不只收录成功。My Current Model 只保存用户实际表达；Core Idea 若由 Tutor 提炼，要明确标记。

### 5.2 Templates/session.md

frontmatter：

```yaml
---
date: YYYY-MM-DD
thread: concept-slug
status: active
mode: learn
---
```

原有五个摘要栏目保留。增加两个栏目：

`## 恢复检查点`

- 当前位置：
- 已提供帮助：
- 最后真实输出：引用本文件证据片段，或写“尚未输出”。
- 待完成：
- 关联记录：使用相对链接。

“下次第一件事”沿用已有栏目，作为唯一的恢复动作位置，不在检查点复制另一份。

`## 关键学习证据（可选）`

每个片段使用普通 Markdown，必要时编号为 `### 证据 1` 等，不引入 UUID：

```text
类型：assisted / reconstruction / independent / delayed-independent
目标维度：
题目或任务：
原始回答：使用引用块逐字保存关键输出；节选注明节选
实际帮助：包括前置教学与作答中提示
无查阅条件：用户自述 / 未确认 / 不适用
作答前置信度：0–100 / unknown / 不适用
novel_transfer：yes / no / unknown
结果：correct / partial / incorrect / uncertain
判断依据：关键正确处、缺口；有来源则定位
关联：Concept / Paper / Mistake；延迟测试注明对应前次学习记录
```

每次活动通常保留 1–2 个最有价值的片段，但不能以数量限制为由删除关键反证。没有 learner attempt 时不创建虚假证据片段。

关联到具体证据可用 Session 链接并注明“证据 1”；不要求自动锚点生成器。

### 5.3 Templates/paper.md

frontmatter 保留 `status: reading`、`thread`、`next_action`，不扩展论文状态机。`next_action` 是下一动作的唯一位置；原 `## Next Step` 合并进它，避免两份下一步冲突。

增加 `## Source`：

- 标题与已核实的版本信息；
- 本地 PDF 相对链接或原始来源 URL；
- DOI / arXiv 标识仅在已知时填写；
- 页码使用 PDF 页序还是印刷页码，必要时说明。

Current Position 继续保存当前节、页、公式或图表，不和 Source 混用。

合并独立空白 P0/P1/P2/P3 栏目到 `## Current Blockers`，每个 blocker 一处记录优先级、类型、原文位置、需要的最小补充、状态和验证证据。使用 open / resolved-for-reading / deferred；checkbox 仅在 resolved-for-reading 时勾选。

增加 `## Claims / Evidence / Verification`，仅针对重要主张填写：

- 作者主张及位置；
- 原文证据与适用范围；
- 我的判断：必须来自用户实际输出，否则写尚未输出；
- Tutor 建议：如有，明确归属；
- 下一验证动作：具体对照、推导检查或来源核验；
- 结论状态：尚未验证 / 有限支持 / 不支持 / 不确定，并解释依据。

保留其他有用栏目。不得要求每篇论文、每个公式都填满全部字段。

### 5.4 Templates/mistake.md

保留现有 frontmatter 和正文。增加：

- `## 证据来源`：Session 片段链接。
- `## 最近复测`：日期、题目或链接、实际帮助、结果；没有则写未复测。

不新增 Mistake 状态字段。推荐复测时读“最近复测”和“下次怎么测试”，不依赖尚不存在的 open 状态。

沿用现有 `reoccurrences` 字段拼写，不做无收益的改名。定义为已记录的有效出现总次数，首次为 1；重复整理同一次事件不增加。

同一模式复发时追加来源和必要例子，不覆盖旧原始回答。已有原始回答可迁移为保留的历史例子；不能只剩“最近一次”而丢失此前证据。

## 6. SPEC 与 Skill 的组织

SPEC 负责定义，Skill 负责操作。避免同一掌握门槛、字段表、证据类型在多个文件完整复制。

Skill 至少应形成以下操作顺序：

1. 判断本次意图及模式，确定 thread。
2. 只读取相关状态；无记录不推定不会。
3. browse 直接解释；learn 提供最低有效帮助；assess 按正式 A0 条件执行。
4. 对实际 learner attempt 给出有依据的反馈，保留原话。
5. 阶段结束或暂停时先保存 Session，按证据更新相关记录。
6. 给出下一具体动作；存在真实歧义才询问。

必须删除或改写以下旧规则冲突：

- 所有 A5 无差别强制重建 → 核心学习邀请重建，浏览及明确暂停不强制，未完成保持未验证。
- 补课后原题直接记 A0 → 记 reconstruction。
- 格式明显无效就停止所有教学并让用户先修复 → 按 4.7 进行局部处理。
- 新概念无条件先确认名称 → 明确名称直接创建，歧义才询问。
- 必须先得到 reflection 再写 Session → 可先写真实检查点，reflection 可选。
- 评分和提升无需具体模板证据 → 按第 4、5 节追溯证据。
- 以 open Mistakes 为推荐输入但没有状态字段 → 根据最近复测和待测试内容判断。

README 增加六个简短例子：快速浏览、核心学习、正式测试、暂停保存、明确 thread 恢复、选择今日一个动作。说明用户不需要记 mode、证据类型或任何命令。

## 7. 实施顺序

1. 读当前 SPEC、Skill、模板、README 和工作区变更，确认有无新增真实笔记。
2. 按本方案更新 SPEC，先固定语义与边界。
3. 更新四个模板，检查字段、取值及标题与 SPEC 对齐。
4. 更新 Skill，消除第 6 节冲突，落实写入顺序和恢复行为。
5. 更新 README；AGENTS 仅作必要的短入口调整。
6. 如发现已有真实记录，执行第 9 节最小兼容处理。
7. 执行第 10 节验收，修正发现的问题。
8. 创建 acceptance-report.md，报告实际文件变更、验收结果、限制及未完成项。

不把本轮实施扩大成新项目。受阻部分要明确标注，其他互不依赖的工作继续完成。

## 8. 例子仅用于说明，不是用户记录

以下为虚构设计例子，禁止写入真实 Concepts、Papers、Mistakes 或 Sessions。

场景：学习强凸性。

1. 用户要“直接讲一下”，Tutor 给 A5。
2. Tutor 请用户不看解释，说明强凸性比凸性多约束了什么。
3. 用户尚未回答便暂停：Session 记录先前 A5 和待 reconstruction；四维不因解释自动提高。
4. 下次用户给出输出：保存 reconstruction 证据；根据真实表现决定继续练习。
5. 几天后使用未泄露解法的新任务测试：合格时保存 delayed-independent；若只是照搬旧题，则不标陌生迁移。
6. 是否 mastered 仍要检查维度覆盖、跨日证据、陌生迁移和反证。

## 9. 兼容与回退

实施前重新盘点。只有模板而没有真实笔记时，修改模板即可，不预建学习记录。

对已有笔记：

- 缺少新可选栏目是兼容情况，不应阻断使用；下次实际更新时补齐。
- 历史 `0` 不批量改成 null；缺少依据时标“历史评分未核验”，不据此推断不会。
- 历史 mastered 若缺证据，注明“历史掌握判断待核验”，不得伪造补证；明确标记核验前不作为新掌握结论使用。
- 原有 thread 与引用优先保留；确需修正名称时先查所有引用，避免断链。
- 没有原始输出的历史总结可以保留为历史摘要，不能升级为正式独立证据。
- 模式不明的旧 Session 不按当前偏好补猜；正文标明未记录即可。

需要回退时，只撤销本轮实际新增或修改的片段；使用执行前备份或逐文件补丁，不能回退到仓库旧版本并覆盖用户基线。临时备份放工作区外的允许临时目录，交付不遗留重复副本。

## 10. 验收方法与固定场景

### 10.1 验收层级

1. 静态检查：文件是否满足本方案，字段、定义、引用是否一致。可使用现成文本与 YAML 工具，无须引入依赖或常驻测试框架。
2. 行为演练：让执行环境中的 Tutor 依据修改后的规则处理下列固定输入，检查响应及持久化结果。优先使用临时副本，所有模拟数据标记 synthetic。
3. 真实学习效果：交付后由实际学习积累证据，本轮不冒充完成。

如无法实际启动行为演练，只能报告静态检查通过、行为验收未执行；不能把“读起来应该如此”记为 PASS。不要通过新增插件或多 Agent 基础设施来完成验收。

### 10.2 固定场景

| 编号 | 输入或前置条件 | 必须观察到的行为 |
|---|---|---|
| T01 | 新概念无记录，用户想开始学习 | 四维与 ai_level 默认为 null；不宣称不会；无歧义不先确认 slug |
| T02 | “这个符号是什么意思？只解释，不测试” | 直接解释，不催重建，不改掌握分数 |
| T03 | 核心概念完整解释后用户说“懂了” | 邀请重建；没有输出前不提高能力判断 |
| T04 | A5 后原题无提示复述正确 | 标 reconstruction，写前置 A5，不标正式 A0 |
| T05 | 正式新题测评 | 先展示题目，再收作答前 confidence，答题前不泄露解法 |
| T06 | 正式测评中途请求提示 | 保留此前输出，后续记实际帮助；完整成功不算独立成功 |
| T07 | 一次独立答对但无跨日或迁移证据 | 不写 mastered |
| T08 | 两天的独立证据、关键维度与陌生迁移齐备且无反证 | 可有依据地标 mastered，写范围和链接；门槛不作为自动评分器 |
| T09 | A5 后用户说“先停，帮我保存” | 保存 paused 检查点及待重建，不能要求先反思才能保存 |
| T10 | 按明确 thread 恢复上述活动 | 直接接待重建任务，不重讲整节，不猜用户理解 |
| T11 | 两个合理 thread，用户说“继续昨天的学习” | 一次简短消歧，不按最近修改时间选择 |
| T12 | 论文补课后原文重建正确 | 可解除当前 blocker；不把基础概念直接标 mastered |
| T13 | 论文补课后用户未输出或选择跳过 | 保持 open 或 deferred，不勾选 solved |
| T14 | 同一稳定错误再次出现；随后重复整理同一次记录 | 复用文件，新事件计数一次，保留旧输出，整理不额外计数 |
| T15 | 旧记录缺 mode 或 Evidence 栏目 | 兼容读取，不停止所有教学，不伪造历史字段 |
| T16 | 不可解析 frontmatter 或 thread 冲突 | 不覆盖、不猜相关状态；可继续无关解释，明确局部问题 |
| T17 | AI 无法可靠判定一段推导正确性 | 记 uncertain，不提升分数，指明需要核查的条件或来源 |
| T18 | “今天最值得学什么”，同时有当前 P0 和一般到期复习 | 结合目标给一个动作及依据，不列大课程表 |
| T19 | 学习者不同意评分，或缺少置信度 | 接受重新核验；不强迫报数、不编造 confidence |
| T20 | Source 缺版本/DOI；Critique 尚无实验 | 写 UNKNOWN；明确推断与待验证动作，不宣称已验证 |
| T21 | Session 成功写入，但 Concept 更新失败 | 原始证据仍在；报告部分保存，不假称全部完成 |
| T22 | 复习到期但用户没有参加 | 只保持待复习，不自动降分或记录错误 |

T08 的完整证据可使用明确 synthetic 的临时夹具，不能靠造用户历史通过验收。T21 可做受控故障演练；若环境不支持，注明未运行及仅静态检查到的顺序，不能假记通过。

### 10.3 文档与工作区检查

- 四份模板 YAML 可解析；null 为真正空值，不是字符串 "null"。
- 新建记录使用固定路径及 lowercase-kebab-case；日期、thread 和文件名一致。
- Concept 证据链接指向存在的 Session；演练中的相对链接有效。
- 不存在“所有 A5 都必须完成重建才能暂停”“原题重建等于 A0”等残留冲突。
- 不把未评估、未作答、未复习或暂缓等同于错误。
- Paper 下一动作只有一处权威字段；blocker 不在两个栏目重复维护。
- 原有 PDF 和用户变更未被覆盖；仓库不残留 synthetic 学习数据。
- 没有引入新依赖、自动化、旧代码恢复或自动推送。

## 11. 执行者交付格式

`acceptance-report.md` 必须包含：

1. 执行日期和基线说明。
2. 实际修改文件及每个文件的用途。
3. T01–T22 逐项结果：PASS / FAIL / NOT RUN，附观察到的响应或文件证据；不以方案预期替代实际结果。
4. 静态检查结果，以及行为演练的工具、隔离方式和限制。
5. 兼容处理：发现了哪些旧记录，实际做了什么；没有则明确写无。
6. 未解决项、权限阻塞或与本方案的偏离及理由。
7. 清楚结论：文档修改是否完成、行为验收是否完成、学习效果尚待真实使用验证。

任一必须项 FAIL 或关键行为 NOT RUN 时，不得给出“全部验收通过”。可以交付已完成工作，但必须明确剩余项。

## 12. 后续一至两周的实际使用观察

本节是建议的使用验证，不授权自动创建提醒，也不是本轮交付必须等待完成的事项。

利用真实 Session 观察三个问题：

- 恢复是否顺畅：重新打开时能否直接继续，不用复述背景？
- 独立表现是否出现：是否有跨日新题输出；若没有，原因是什么？
- 记录是否打断学习：哪些询问或栏目反复造成负担？

用户愿意时再做一次短回顾，按实际例子调整；不要先建立分数看板或统计系统。短期观察只能帮助改进流程，不能证明系统导致了学习能力提升。
